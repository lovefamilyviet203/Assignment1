import java.util.Stack;

public class StackInitialization {
    public static void main(String[] args) {
        // Initialize a stack of type String
        Stack<String> stack = new Stack<>();

        // You can now use this "stack" variable to perform stack operations.
        // For example, you can push elements onto the stack or check if it's empty.

        // Push elements onto the stack
        stack.push("A");
        stack.push("B");
    }
}
